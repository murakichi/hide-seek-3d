---
name: review-prs
description: このリポジトリの open な PR をレビューし、問題がなければマージする。CI の結果と差分を確認し、勝率の退行や決定論の破壊がないかを見てから判断する。/loop と組み合わせて、improve-ai が作った PR を継続的に取り込むのに使う。マージ後のブランチ整理まで行う。
---

# PR のレビューとマージ

`/improve-ai` などが作った PR を確認して取り込む。
**中身を読まずにマージしないこと。** CI が緑でも、勝率の退行や決定論の破壊は CI では落ちない。

## 手順

### 1. open な PR を一覧する

```bash
gh pr list --state open
```

無ければ何もせず終了する（「PR はありません」とだけ報告する）。
複数あるときは古い順に 1 件ずつ処理する。

### 2. 中身とマージ可否を確認する

```bash
gh pr view <番号>
gh pr diff <番号>
gh pr checks <番号>
gh pr view <番号> --json mergeable,mergeStateStatus -q '.mergeable + " / " + .mergeStateStatus'
```

CI が走っていない、または失敗しているなら、ログを見て原因を書く。
失敗したままマージしない。

`mergeable` が `CONFLICTING` なら手順 2.5 へ。`UNKNOWN` は GitHub が判定中なので、
少し待ってから引き直す。

### 2.5 コンフリクトを解決する

改善サイクルを並行して回すと、`src/ai` の同じ関数を複数の PR が触って必ず衝突する。
**PR の作者に投げ返さず、ここで解決する**（`/loop` で回している以上、投げ返す相手がいない）。

```bash
git fetch origin
git checkout <ブランチ名>
git merge origin/master
```

解決の方針はファイルによって違う。

- **`docs/journal/*.md`、`docs/balance-log.md`** — どちらも追記なので**両方残す**。
  時刻順に並べ直すだけでよい。片方を捨てない。
- **GitHub issue** — 同じ issue を両方が更新していたら、観測した事実は両方残す。
  status は「片方が closed なら closed」。
- **`src/ai` のロジック** — 両方の意図を保つ。片方の変更を落とすと、その PR が
  報告している勝率が意味を失う。どうしても両立しないなら、**新しい方を採らずに**
  マージを中止し（`git merge --abort`）、PR にどちらを取るべきか判断を仰ぐコメントを残す。
- **`src/ai/tuned.json`** — 自動探索の出力なので機械的にはマージできない。
  片方を選んだうえで、手順 3 の再測定を必ず行う。

```bash
git add -A && git commit --no-edit
npm run build          # 型が通ることを先に確かめる
git push
```

**解決したら勝率を測り直す。** これが最も重要。2 つの変更が個別に良くても、
組み合わせると悪化することがある（例: 片方が逃走方向の選び方を変え、
もう片方が速度を変えていると、先読み距離の前提が崩れる）。

```bash
npm run sim -- --games 24 --hiders 1 --seekers 1
npm run sim -- --games 24 --hiders 2 --seekers 2
npm run sim -- --games 24 --hiders 3 --seekers 3
```

PR 本文の数字と食い違ったら、実測値を PR にコメントで残し、
退行しているならマージせず保留する。

### 3. 差分を審査する

このプロジェクト固有の観点で見る。一般的なレビューよりここが重要。

- **`src/core` の決定論が壊れていないか。** `Math.random()`、`Date.now()`、
  実時間への依存が入っていないか。入っていればマージせず指摘する。
- **`src/core` が `three` や DOM を参照していないか。** ヘッドレス実行が壊れる。
- **勝率が報告されているか。** AI やバランスを触る PR は、本文に
  1v1 / 2v2 / 3v3 の数字と前回値があるはず。無ければ求める。
  ±10 ポイント以内の差を「改善」と主張している場合は、試合数を増やして測り直させる。
- **速度や視界を変えた PR で、AI の先読み距離も一緒に見直されているか。**
  片方だけだと AI が壁に突っ込むようになる（過去に実際に起きた）。
- **`docs/journal/` と GitHub issue が更新されているか。** 記録が無い変更は、
  次のサイクルで同じ調査を繰り返すことになる。

### 4. 判断する

- **問題なし** → マージする。
  ```bash
  gh pr merge <番号> --merge --delete-branch
  ```
  master への push で Pages が自動デプロイされる。
- **直せば通る軽微な問題** → 自分で直す。PR のブランチに追加コミットして push し、
  CI が通ってからマージする。何を直したかは PR にコメントで残す。
  ```bash
  gh pr comment <番号> --body "..."
  ```
- **設計の判断が要る / 勝率が退行している** → マージせず、`gh pr comment` で
  懸念を具体的に書く。何をどう確かめれば判断できるかまで書く。

### 5. 後始末

マージしたら master を最新にし、対応する issue が閉じられているか確認する。

```bash
git checkout master && git pull
gh pr list --state open
```

## 報告

処理した PR ごとに、番号・タイトル・判断（マージ / 保留）・理由を 1 行で報告する。
保留したものは何が足りないかを明示する。
コンフリクトを解決したなら、何と何が衝突してどう解決したか、
再測定した勝率が PR の申告とどう違ったかを添える。
